# Data — use HF datasets instead
data/raw/
data/processed/
*.wav
*.flac
*.mp3

# Jupyter
.ipynb_checkpoints/

# Secrets — never commit (template is .env.example, tracked)
.env

models/checkpoints
.venv
